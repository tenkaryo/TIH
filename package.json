{
  "name": "onthisday-api",
  "version": "1.0.0",
  "description": "API server for OnThisDay historical data",
  "main": "api.js",
  "scripts": {
    "start": "node api.js",
    "dev": "nodemon api.js",
    "build": "echo \"Static files ready for deployment\"",
    "deploy": "vercel --prod",
    "local": "concurrently \"npm start\" \"python3 -m http.server 3000\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "history",
    "api",
    "onthisday"
  ],
  "author": "OnThisDay Team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}